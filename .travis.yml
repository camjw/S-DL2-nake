language: cpp

before_install:
- sudo aptitude install -y  libsdl2-dev
- sudo aptitude install -y  libsdl2-mixer-dev
- sudo aptitude install -y  libsdl2-ttf-dev

script:
  - make
  - make test
  - ./test

compiler:
  - g++
